function showNewTaskForm() {
    // needs to show a pop-up box to fill in
    const overlay = document.getElementById("popupOverlay");
    overlay.classList.add("show");

}

export default showNewTaskForm;